import Tilt from "react-tilt";
import motion from 'framer-motion';

import styles from '../styles';
import github from '../assets';
import projects from '../utils/motion';


const Works = () => {
  return (
    <div>Works</div>
  )
}

export default SectionWrapper(Works, "works")